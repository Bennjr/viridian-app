## To start the server
To start the server you will need to have rust and cargo installed as well as node, prefferably pnpm which can be installed through npm (Node package manager) to avoid conflicts. 
After all dependencies are installed you will need to run 
``` bash
pnpm tauri dev
```
